# Copia este archivo a .env.local y rellena los valores reales.
# .env.local está en .gitignore y NUNCA debe subirse al repo.

# ── Supabase ────────────────────────────────────────────────────────────────
# Settings → API en tu proyecto de Supabase
NEXT_PUBLIC_SUPABASE_URL=https://TU-PROYECTO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-anon-key

# ── Gemini (Google AI Studio) ───────────────────────────────────────────────
# https://aistudio.google.com  → Get API key
GEMINI_API_KEY=tu-gemini-api-key

# Opcionales — modelos por defecto si no se definen:
# GEMINI_MODEL=gemini-2.5-flash            # texto (plan, recetas, conteo por foto)
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image # imagen (foto IA del plato; requiere billing)

# ── Notificaciones push (recordatorios) ─────────────────────────────────────
# Genera el par con:  node -e "console.log(require('web-push').generateVAPIDKeys())"
NEXT_PUBLIC_VAPID_PUBLIC_KEY=tu-vapid-public-key
VAPID_PRIVATE_KEY=tu-vapid-private-key
# Secreto que protege el cron (cualquier string aleatorio largo)
CRON_SECRET=un-secreto-aleatorio
# Service role de Supabase (Settings → API). SOLO server. Lo usa el cron para
# leer suscripciones y registros de todos los usuarios (omite RLS).
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key
